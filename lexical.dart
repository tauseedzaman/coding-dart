var add = (x, y) => x + y;
var sub = (x, y) => x - y;
var div = (x, y) => x / y;
var mul = (x, y) => x * y;
void main() {
  var x = 5;
  var y = 10;
  print(add(x, y));
  print(sub(x, y));
  print(div(x, y));
  print(mul(x, y));
}
